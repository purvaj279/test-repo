services:
  - type: web
    name: wanderlore
    env: python
    buildCommand: pip install -r requirements.txt && python database.py
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
